# Development / test dependencies. Install with:  pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1        # ASGI transport for the API tests
ruff==0.16.4         # lint + format
amqtt==0.12.0        # in-process MQTT broker for the end-to-end test in tests/test_mqtt.py
